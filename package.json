{
  "name": "speedreader-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter speedreader dev",
    "dev:web": "bun run --filter speedreader dev",
    "dev:mobile": "bun run --filter speedreader-mobile dev",
    "build": "bun run build --filter @speedreader/core && bun run build --filter speedreader",
    "build:web": "bun run build --filter @speedreader/core && bun run build --filter speedreader",
    "build:mobile": "bun run build --filter speedreader-mobile",
    "lint": "bun run --filter '*' lint",
    "test": "bun run --filter '*' test",
    "prepare": "bun run build --filter @speedreader/core"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
